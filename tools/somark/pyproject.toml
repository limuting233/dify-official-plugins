[project]
name = "somark"
version = "0.0.1"
description = "SoMark document extraction plugin"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dify_plugin>=0.5.0",
    "requests>=2.32.5",
]

[dependency-groups]
dev = []
